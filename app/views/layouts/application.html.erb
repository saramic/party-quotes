<!DOCTYPE html>
<html>
  <head>
    <title>PartyQuotes</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>

    <%= yield :head %>
  </head>

  <body>
  <div class="box-border bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 h-screen">
  <div class="flex flex-col">
    <%= yield %>
  </div>
  </div>
  </body>
</html>
